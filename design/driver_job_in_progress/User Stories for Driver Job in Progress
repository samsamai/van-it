User Stories for Driver Job in Progress

- A driver can see a map with rider location, directions, ETA
- A driver can call the rider
- A driver can message a rider to ask a question
- A driver can receive a message from a user
- A driver can receive a cancellation notification
- A driver can click Job Start
- A driver can see a map with destination location, directions, ETA
- A driver can mark a ride as 'finished'
